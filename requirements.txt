flask>=3.0
faster-whisper>=1.0
pyloudnorm>=0.1
soundfile>=0.12
numpy>=1.24
mutagen>=1.45      # MP3 ID3 chapter markers (M4A chapters use ffmpeg)
fpdf2>=2.7         # PDF document export (transcript + summaries)
python-docx>=1.1   # Word (.docx) document export

# Optional – speaker diarization (requires HuggingFace token):
# pyannote.audio>=3.1
# torch>=2.0

# openai-whisper is NOT required — faster-whisper is used instead.
# openai-whisper can be installed as a fallback if faster-whisper is unavailable:
# openai-whisper>=20231117

# Note: ffmpeg must be installed separately (noise reduction uses ffmpeg's built-in afftdn):
#   macOS:  brew install ffmpeg
#   Ubuntu: sudo apt install ffmpeg
